name: Build TonTonDeck

on:
  push:
    branches: [main, master]
    tags:
      - 'v*'
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev

      - name: Install npm dependencies
        run: npm ci

      - name: Build Tauri app
        run: npm run tauri build

      - name: Upload AppImage
        uses: actions/upload-artifact@v4
        with:
          name: TonTonDeck-linux-appimage
          path: src-tauri/target/release/bundle/appimage/*.AppImage
          if-no-files-found: error

  build-flatpak:
    runs-on: ubuntu-latest
    container:
      image: bilelmoussaoui/flatpak-github-actions:gnome-46
      options: --privileged
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Flatpak
        uses: flatpak/flatpak-github-actions/flatpak-builder@v6
        with:
          bundle: TonTonDeck.flatpak
          manifest-path: flatpak/com.tontondeck.app.yml
          cache-key: flatpak-builder-${{ github.sha }}

      - name: Upload Flatpak
        uses: actions/upload-artifact@v4
        with:
          name: TonTonDeck-linux-flatpak
          path: TonTonDeck.flatpak
          if-no-files-found: error

  # TODO: Uncomment when ready to build for macOS
  # build-macos:
  #   runs-on: macos-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: 'npm'
  #
  #     - name: Install Rust
  #       uses: dtolnay/rust-toolchain@stable
  #
  #     - name: Rust cache
  #       uses: Swatinem/rust-cache@v2
  #       with:
  #         workspaces: src-tauri
  #
  #     - name: Install npm dependencies
  #       run: npm ci
  #
  #     - name: Build Tauri app
  #       run: npm run tauri build
  #
  #     - name: Upload macOS app
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: TonTonDeck-macos
  #         path: src-tauri/target/release/bundle/macos/*.app
  #         if-no-files-found: error
  #
  #     - name: Upload macOS dmg
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: TonTonDeck-macos-dmg
  #         path: src-tauri/target/release/bundle/dmg/*.dmg
  #         if-no-files-found: ignore

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install npm dependencies
        run: npm ci

      - name: Build Tauri app
        run: npm run tauri build

      - name: Upload Windows exe
        uses: actions/upload-artifact@v4
        with:
          name: TonTonDeck-windows-exe
          path: src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error

      - name: Upload Windows msi
        uses: actions/upload-artifact@v4
        with:
          name: TonTonDeck-windows-msi
          path: src-tauri/target/release/bundle/msi/*.msi
          if-no-files-found: ignore

  # Create release when pushing a tag
  release:
    needs: [build-linux, build-flatpak, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/**/*.AppImage
            artifacts/**/*.flatpak
            artifacts/**/*.exe
            artifacts/**/*.msi
          generate_release_notes: true
          draft: false
          prerelease: false
